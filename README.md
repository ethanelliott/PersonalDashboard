# PersonalDashboard


## Build

Make sure you have yarn and npm installed
```
npm install
yarn dist
```

## Run locally
Run the project without compiling an executable.

```
npm install
npm start
```
